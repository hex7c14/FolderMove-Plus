# 项目级 pnpm 配置（pnpm v11+ 不再读 package.json 的 pnpm 字段）
# 允许依赖运行构建脚本（如 esbuild 下载预编译二进制、@tauri-apps/cli 下载 Rust CLI、
# @resvg/resvg-js 下载 Rust 预编译二进制）
# 说明：pnpm v10+ 出于供应链安全默认会拦截所有未批准的 build 脚本，
# 这里因为全是 esbuild/vite/tauri 等广泛使用的知名开源依赖，
# 对 GPL 本地开发项目放开是可接受的且必要的，否则无法构建。
allow-build-scripts=true
